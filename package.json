{
  "name": "siberboard",
  "version": "1.0.0",
  "private": true,
  "description": "SiberBoard — visual flow & flowchart builder (Datasiber Lab)",
  "scripts": {
    "build": "node build.mjs",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tailwindcss": "^3.4.17"
  }
}
